version: 2

# Build from docs/ directory with sphinx
sphinx:
  configuration: WarThunder/docs/conf.py

# Explicitly set the version of Python and its requirements
python:
  version: 3.7
  install:
    - requirements: WarThunder/docs/requirements.txt
